# @zjournal/ui-library

UI component library for zJournal. Includes Storybook stories for local development.

Quick start:

```bash
cd ui-library
npm install
npm run storybook
```

Build:

```bash
cd ui-library
npm run build
```
