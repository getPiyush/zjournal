# Placeholders for the future Google OAuth admin gate (see src/admin/authConfig.ts).
# Unused for now — /admin currently has no login gate.
REACT_APP_GOOGLE_CLIENT_ID=
REACT_APP_ADMIN_EMAILS=

# Where the standalone analytics service (../analytics) listens; article views are reported
# here. Defaults to http://localhost:4400 in src/properties.js if unset.
REACT_APP_ANALYTICS_URL=http://localhost:4400
